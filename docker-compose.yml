services:
  claude-morning:
    build: .
    volumes:
      - ./data:/root/.claude
      - .:/workspace
    command: daemon
    environment:
      - CLAUDE_CRON_SCHEDULE=0 8 * * *
    restart: unless-stopped
