services:
  claude-morning:
    build: .
    volumes:
      - ./data:/root/.claude
      - .:/workspace
    command: daemon
    environment:
      - CLAUDE_MORNING_CRON_SCHEDULE=0 8 * * *
      - CLAUDE_MORNING_TZ=Asia/Bangkok
    restart: unless-stopped
